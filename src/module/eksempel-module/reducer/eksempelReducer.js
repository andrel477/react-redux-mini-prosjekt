const defaultState = {
  test: 'test',
};

const eksempelReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default eksempelReducer;
